.venv/
__pycache__/
*.pyc
.pytest_cache/

# large artifacts — never commit corpus, indexes, or model weights
data/raw/
data/processed/
indexes/
*.faiss
*.index
models/
cache/

# results (regenerate via eval scripts, but keep small summary CSVs if desired)
results/*.json
!results/.gitkeep

.env
.DS_Store
