# --- Retrieval (Days 1-2) ---
pyserini==0.22.1          # BM25 via Anserini/Lucene (requires Java 11+)
faiss-cpu==1.8.0          # dense vector index (flat, exact search)
sentence-transformers==3.0.1   # bge-small-en-v1.5 encoder
rank_bm25==0.2.2          # pure-python BM25 fallback (no JVM), used in tests

# --- Optional: ONNX Runtime backend for faster CPU dense encoding ---
# Typically 2-3x faster than plain PyTorch on CPU, same weights/quality,
# free and open-source (no cloud dependency). Enable with `--onnx` on
# data/preprocess.py. Not installed by default to keep the base install
# light; install manually if you want to try it:
#   pip install optimum[onnxruntime] "sentence-transformers>=3.2"

# --- Reranking (Day 3) ---
# BAAI/bge-reranker-base loaded via sentence-transformers CrossEncoder

# --- Ranking / LTR (Days 5-7) ---
xgboost==2.0.3
shap==0.52.0
matplotlib==3.10.8
scikit-learn==1.5.0

# --- Evaluation (all days) ---
pytrec_eval==0.5

# --- QA (Day 8) ---
openai==2.44.0            # OpenAI API client (gpt-4o-mini) — see qa/grounded_qa.py for why
                            # this replaces the originally-planned AWS Bedrock/Claude Haiku
pydantic==2.13.4          # structured-output schema for QAResponse

# --- Shared ---
numpy==1.26.4
pandas==2.2.2
tqdm==4.66.4
pyyaml==6.0.1

# --- Dev / testing ---
pytest==8.2.2
